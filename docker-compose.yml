version: "3.7"
services:
    visible-vowels:
       image: "${APPNAME}:${VERSION}"
       deploy:
          replicas: 1
          resources:
             limits:
                memory: "3GB"
       ports:
          - "10000:3838"
       networks:
          - ${APPNAME}
networks:
    visible-vowels:
